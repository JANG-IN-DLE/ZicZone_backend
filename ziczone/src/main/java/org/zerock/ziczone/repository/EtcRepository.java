package org.zerock.ziczone.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.zerock.ziczone.domain.Etc;

public interface EtcRepository extends JpaRepository<Etc, Long> {
}
